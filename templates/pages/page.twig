{% extends "base.twig" %}
{# {% block header %}
<header class="header container headerContainer">
  {% include 'navheader.twig' %}
</header>
{% endblock %} #}

{% block content %}
  <div class="row contentBody">
    {% if post.meta('content_selector') %}
      {% for contentBlock in post.meta('content_selector') %}
          {% if contentBlock.acf_fc_layout == "left_half" %}
              {% include 'layouts/lefthalf.twig' %}              
          {% elseif contentBlock.acf_fc_layout == "right_half" %}
              {% include 'layouts/righthalf.twig' %}
          {% elseif contentBlock.acf_fc_layout == "full_screen" %}
              {% include 'layouts/fullscreen.twig' %}                    
          {% endif %}
      {% endfor %} 
    {% else %}
      {% include 'layouts/noContent.twig' %}                    
    {% endif %}
      {# <div class="contentBody__left col s12 l6">
        <img class="contentBody__image" src="{{ post.thumbnail.src }}" alt="">        
      </div>   
      <div class="contentBody__right col s12 l6">
        <p>
          We create 
          connections
          that reshape 
          perceptions of the
          neo-african
          diaspora.
        </p>
      </div>
      <div class="contentBody__article col s12">
        {{post.content}}
        {# {% echo postMeta %} #}
      </div>
      <!-- <h1 class="title title--pageBanner">{{ post.title }}</h1>
      <article class="col s12 m8 contentBody__content post-type-{{post.post_type}}" id="post-{{post.ID}}">
        <section class="article-content">
          <div class="article-body">
            {{post.content}}
          </div>
        </section>
      </article>   -->
  </div>
{% endblock %}

{#     <section id="content" role="main" class="content-wrapper">
      {% if title %}<h1>{{title}}</h1>{% endif %}
      <div class="wrapper {{sidebar_class}}">
        {% block content %}
          Sorry, no content
        {% endblock %}
      </div>
      {% if sidebar %}
        <aside class="layout-sidebar">
          {{sidebar}}
        </aside>
      {% endif %}
    </section> #}
 